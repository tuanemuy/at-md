/**
 * リポジトリリポジトリインターフェース
 * 
 * アプリケーション層のリポジトリリポジトリインターフェース
 */

import { RepositoryRepository as CoreRepositoryRepository } from "../../../core/content/mod.ts";

/**
 * リポジトリリポジトリインターフェース
 */
export interface RepositoryRepository extends CoreRepositoryRepository {
  // 追加のメソッドがあればここに定義
} 